# JOJX site

Static site. `index.html` routes phones to `JOJX Mobile.dc.html`, everything else to `JOJX Home 4.dc.html`.

Deploy: enable GitHub Pages on `main` (root). Note the Vimeo progressive URLs expire — swap for embeds or self-hosted MP4s for permanence.
